---
import { cn } from "#/lib/utils";

interface Props {
  as?: string;
  class?: string;
  [key: string]: unknown;
}

const { as: Tag = "div", class: className, ...rest } = Astro.props;
---

<Tag class={cn("mx-auto w-full max-w-6xl px-6 lg:px-8", className)} {...rest}>
  <slot />
</Tag>
