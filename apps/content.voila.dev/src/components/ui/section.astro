---
import { cn } from "#/lib/utils";

interface Props {
  id?: string;
  class?: string;
  [key: string]: unknown;
}

const { id, class: className, ...rest } = Astro.props;
---

<section id={id} class={cn("py-20 sm:py-28", className)} {...rest}>
  <slot />
</section>
