---
import { Icon as Iconify } from "astro-icon/components";

interface Props {
  class?: string;
  [key: string]: unknown;
}

const { class: className, ...rest } = Astro.props;
---

<Iconify name="simple-icons:resend" class={className} role="img" aria-label="Resend" {...rest} />
