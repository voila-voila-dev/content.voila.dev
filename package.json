{
  "name": "content.voila.dev",
  "private": true,
  "workspaces": {
    "packages": [
      "apps/*",
      "packages/*",
      "examples/*",
      "scripts/*"
    ],
    "catalog": {
      "react": "19.0.0",
      "react-dom": "19.0.0",
      "@types/react": "19.0.1",
      "@types/react-dom": "19.0.1",
      "vite": "^7"
    }
  },
  "scripts": {
    "prepare": "lefthook install",
    "lint": "biome lint --write",
    "format": "biome format --write",
    "test": "bun test",
    "test:coverage": "bun test --coverage",
    "test:cov:gates": "bun scripts/coverage-gate packages/content 80 && bun scripts/coverage-gate packages/content 90 config/schema && bun scripts/coverage-gate packages/content-ui 90",
    "check": "biome check --write && tsc -b && bun test",
    "changeset": "changeset",
    "version": "changeset version",
    "release": "bun scripts/publish",
    "release:dry": "bun scripts/publish --dry-run"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.15",
    "@changesets/changelog-github": "^0.7.0",
    "@changesets/cli": "^2.31.0",
    "@types/bun": "^1.3.14",
    "lefthook": "2.1.8"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "packageManager": "bun@1.3.14",
  "engines": {
    "bun": ">=1.3.14"
  }
}
