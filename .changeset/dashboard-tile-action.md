---
"@voila/content-ui": patch
---

Dashboard tiles and `StatCard`: the quick "New" action / icon sits in `Card.Action` on the title row again (the kit's card header is a grid, so `flex-row` no longer aligned it).

